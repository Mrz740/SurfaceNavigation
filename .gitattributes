# Shell scripts (git hooks in particular) must always check out with LF line endings,
# regardless of core.autocrlf — a CRLF shebang line silently breaks POSIX shell execution.
.githooks/* text eol=lf
*.sh text eol=lf
